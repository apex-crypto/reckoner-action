name: Reckoner
description: "Run a reckoner command"

inputs:
  reckoner-command:
    description: "reckoner command"
    required: true
    default: "reckoner --help"

outputs:
  time:
    description: "The time "

runs:
  using: docker
  image: "docker://public.ecr.aws/i9h4d0i0/apex-crypto/reckoner-action:redesign"
  args:
    - ${{ inputs.reckoner-command }}
